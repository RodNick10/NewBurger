<h1>Burger Time!</h1>

<!--<h2>Choose your Burger's Name!</h2>

<ul>
  {{#each cats}}
    {{#unless sleepy}}
      {{> cats/cat-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Choose your Burger's Name!</h2>

<ul>
  {{#each cats}}
    {{#if sleepy}}
      {{> cats/cat-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>-->

<h2>choose your burger's Name</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="slee">Tasty Name?</label><br>
    <input type="radio" name="sleepy" value="1" checked> Delicious!<br>
    <input type="radio" name="sleepy" value="0"> Yooo what a name...
  </div>

  <button type="submit">Add burger</button>
</form>